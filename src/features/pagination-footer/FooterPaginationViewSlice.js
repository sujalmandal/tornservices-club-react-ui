import { createSlice } from '@reduxjs/toolkit';


export const footerPaginationViewSlice = createSlice({
    name: 'footerPaginationViewSlice',
    initialState: {
      
    },
    reducers: {
      
    }
  });

export const {} = footerPaginationViewSlice.actions;
export default footerPaginationViewSlice.reducer;